# from app import create_app, db
# from app.dao import DeviceDataDao, DeviceDao, LocationDao, UserDao, BrockerDao

# app = create_app()

# def test_device_data_dao():
#     with app.app_context():
#         device_data = DeviceDataDao.get_all()
#         assert device_data is not None
#         assert isinstance(device_data, list)
#         assert len(device_data) > 0
#         print("DeviceDataDao test passed.")

